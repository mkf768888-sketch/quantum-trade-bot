---
tags: [session, v10.9, v10.10, DCI, double-win, macro, q-score]
date: 2026-04-05
version: v10.10.0
duration: ночная сессия ~2:00–3:00 MSK
---

# Сессия 2026-04-05 — DCI финализация + v10.10.0

## Контекст
Продолжение после compaction. Бот на Railway, ~$60 USD суммарно (KC+BB).
4 открытые позиции (max 4), весь торговый капитал занят.

---

## Что было сделано

### 1. Дорожная карта обновлена до v10.9.23
- Прогресс Фазы 1: 65% → **80%**
- Добавлены строки: DCI (Финалится), Health Monitor (Готово)
- Обновлён вводный абзац (упомянуты DCI и hourly monitor)
- Файл: `QuantumTrade_AI_Roadmap_v10.9.html`

### 2. DCI статус (v10.9.23)
Итеративный квест через ошибки ByBit API:
```
"BuyLow"/"SellHigh"  → Invalid parameter: order_type
Убрали orderType     → Invalid parameter: order_type (required)
orderType="Stake"    → dual_assets_extra is required
orderType="Stake" + dualAssetsExtra{} → v10.9.23 ✅ (ждём подтверждения)
```
**Финальная структура тела запроса:**
```python
body = {
    "category": "DualAssets",
    "productId": str(product_id),
    "coin": coin,
    "amount": str(round(amount, 8)),
    "orderType": "Stake",           # ОБЯЗАТЕЛЬНО "Stake", не direction
    "accountType": "FUND",          # ОБЯЗАТЕЛЬНО
    "orderLinkId": order_link_id,
    "dualAssetsExtra": {            # ОБЯЗАТЕЛЬНО nested object
        "selectPrice": str(select_price),
        "apyE8": str(apy_e8),
    },
}
```
KuCoin DCI: **отключён навсегда** — endpoint `400100` per official kucoin-skills-hub docs.

### 3. Анализ логов
- Логи run#12–14: `earnable=$0.00 < threshold=$1.0` — нормально, весь USDT в 4 позициях
- `/health HTTP 200 OK` — Railway healthcheck работает ✅
- DCI цикл ещё не наступал (нужно ~60 мин, $0 свободных USDT)

### 4. v10.10.0 — три апгрейда (задеплоено)

#### Task 1: DXY + S&P500 в macro context
- **API:** stooq.com (бесплатно, без ключей)
  - DXY: `https://stooq.com/q/l/?s=dxy.fx&f=sd2t2ohlcv&h&e=csv`
  - S&P500: `https://stooq.com/q/l/?s=%5Espx&f=sd2t2ohlcv&h&e=csv`
- Парсинг CSV: берём 5-й столбец (Close) из первой строки данных
- dxy_signal: `bearish_usd` (< 100) / `neutral` (< 104) / `strong_usd` (≥ 104)
- Добавлено в: MiroFish market_ctx, /health вывод, DB macro_snapshots extra
- Обработка ошибок: timeout=6s, fallback → `result["dxy"] = None`, не крашит

#### Task 2: ByBit Double Win
- Та же API семья что и DCI: `/v5/earn/advance/`
- Три новые функции:
  - `bybit_double_win_get_products(coin)` — категория `"DoubleWin"`
  - `bybit_double_win_get_positions()` — активные позиции
  - `double_win_auto_place(usdt_amount)` — FUND → fallback UNIFIED
- Env vars: `DOUBLE_WIN_ENABLED=false` (Railway), `DOUBLE_WIN_MIN_INVEST=5.0`, `DOUBLE_WIN_MAX_INVEST=20.0`
- Триггер: `_earn_stats["_dci_cycle"] % 4 == 0` (каждые ~60 мин)
- В /health: `🎰 DW: ✅ вкл | 2 поз | products=12`

#### Task 3: Q-Score Auto-tune
- Новая DB функция `db.get_best_q_threshold()` — SQL по последним 30 дням
- Q-диапазоны: <70 / 70-73 / 74-76 / 77-79 / 80-84 / 85+
- Критерии: минимум 10 сделок, WR > 55%, avg_pnl > 0
- Границы безопасности: **никогда ниже 72 или выше 90**
- Условие срабатывания: разница > 2 пункта от текущего MIN_Q_SCORE
- Per-pair пороги сдвигаются пропорционально дельте
- Триггер: каждые 672 цикла (~7 дней в earn_monitor_loop)
- Telegram: `🎯 Q-Score автонастройка: 77 → 80 (WR=67%, 23 сделки)`

---

## Что СУЩЕСТВУЕТ и работает (не переделывать)
| Компонент | Статус | Версия |
|---|---|---|
| `fetch_macro_context()` | ✅ CoinGecko + stooq | v10.10 |
| `fetch_whale_movements()` | ✅ blockchain.info + Blockchair | v9.2 |
| `fetch_top_traders_positions()` | ✅ ByBit leaderboard | v9.2 |
| `mirofish_simulate()` | ✅ 15 agents, memory в DB | v10.0 |
| `save_fg_to_history()` + `db.get_fg_history(n)` | ✅ | v9.2 |
| `/analyze` command | ✅ deep trade analytics | v9.1 |
| `dci_auto_place_idle()` | ⏳ v10.9.23 ожидаем | v10.9.23 |
| `double_win_auto_place()` | 🔒 DISABLED (Railway) | v10.10 |

---

## Commit
```
e9287a5 — v10.10.0: DXY/S&P500 macro, Double Win, Q-Score auto-tune
363 insertions(+), 3 deletions(-)
```

---

## Что смотреть в логах
```bash
# DXY/S&P500 (каждые 15 мин):
[macro] BTC dom=56.2% MCap=$3.1T DXY=104.32 S&P=5421.03

# DCI подтверждение (когда освободится USDT):
[dci_mon] ✅ auto-placed BuyLow $X.XX USDT APY=Y.YY% F&G=35

# Double Win (после DOUBLE_WIN_ENABLED=true):
[dw] found 12 products, best APY=38.5% (USDT 7D)

# Auto-tune (через ~7 дней):
[autotune] ✅ Q_THRESHOLD: 77 → 80 (WR=67% n=23)
```

---

## Следующие шаги
- [ ] Подтвердить DCI первое успешное размещение (ждём логи)
- [ ] Включить `DOUBLE_WIN_ENABLED=true` в Railway когда DCI стабилен
- [ ] Через неделю проверить auto-tune log
- [ ] v10.11: Telegram Mini App / веб-дашборд
