---
tags: [priorities, active]
date: 2026-04-06
version: v10.12.6
---
# Текущие приоритеты (обновлено 2026-04-06, v10.12.6)

## ✅ Сделано за сессию 2026-04-06

### DCI fix chain
1. **v10.12.2** — re-fetch котировки перед размещением (фиксит "Invalid select price")
2. **v10.12.3** — VIP fallback loop: топ-5 кандидатов, пропускаем VIP-only
3. **v10.12.4** — capital sufficiency check + active position guard ("Amount out of range")
4. **v10.12.5** — Auto-reinvest после расчёта + Yield Router ($5→$20) + Double Win fixes
5. **v10.12.5b** — `/api/telegram/notify` endpoint + `/winrate` команда
6. **v10.12.6** — Funding Rate Arbitrage полный модуль

### Стратегический сдвиг
- Фокус: **пассивный доход** (DCI → Funding Arb → Lending), торговля вторична
- Причина: при ~$310 DCI даёт x3 больше чем spot trading
- Цель: вырасти до $1000+ за счёт пассивного дохода

## 🔴 Наблюдать прямо сейчас
- Проверить что DCI **авто-реинвест** сработал после расчёта в 07:59 UTC (♻️ в Telegram)
- `quantum-bot-monitor` теперь может слать отчёты через `/api/telegram/notify`
- `DOUBLE_WIN_ENABLED=true` уже стоит в Railway ✅

## 🟡 Следующие шаги (по приоритету)

### Пассивный доход (ПРИОРИТЕТ #1)
1. **Включить Funding Rate Arb** — `FUNDING_ARB_ENABLED=true` когда будет свободный капитал ($10+)
   - Команда: `/fundarb` для проверки текущих ставок
   - Ждать: ETH или SOL ставка > 0.01%/8ч (~10% APR)
2. **KuCoin Lending Pro** — автоматическое размещение при rate > 10% APR (Wave 1B)
   - Потенциал: 20-50% APR в пике
   - Реализовать: `kucoin_lending_auto_place()`
3. **Yield Router полный** — сравнение APY между всеми продуктами, авто-ротация

### Аналитика (ПРИОРИТЕТ #2)
4. **`/winrate` проверить** как только бот совершит первые сделки
5. **PnL breakdown** — какие монеты зарабатывают, какие теряют
6. **Hourly Telegram report** — проверить что quantum-bot-monitor работает через /notify

### Архитектура (ПРИОРИТЕТ #3)
7. **Wave 3A: CCXT** — нужны API ключи Binance/OKX (заблокировано)
8. **Snowball продукты ByBit** — аналог DCI для sideways рынка
9. **ByBit API `/fundarb` тест** — проверить live ставки командой `/fundarb`

## 🟢 Работает стабильно (не трогать)
- `earn_monitor_loop` — 15 мин циклы, DCI/DW каждые 4 цикла (~60 мин)
- `dci_check_settlements()` — каждый цикл + auto-reinvest при расчёте ✅ NEW
- `mirofish_simulate()` — 15 агентов, память в PostgreSQL
- Smart Money Router: Arb → Trade → DCI → DW → Earn
- Security hardening v10.11.5: ADMIN_CHAT_IDS, webhook auth, asyncio.Lock

## ⚠️ Важные лимиты и границы
- `MIN_Q_SCORE` = 77 (auto-tune bounds: 72–90)
- `DCI_MAX_INVEST_USDT` = $20 per order
- `DOUBLE_WIN_MAX_INVEST` = $20 per order
- `FUNDING_ARB_MAX_USDT` = $30 per position
- `FUNDING_ARB_MIN_RATE` = 0.0001 (0.01%/8h)
- `MAX_OPEN_POSITIONS` = 2
- `ARB_EXEC_ENABLED` = False (hardcoded до капитала >$500)

## 📋 Railway Variables актуальные
```
DOUBLE_WIN_ENABLED=true ✅
DCI_ENABLED=true ✅
DCI_DIRECTION=Auto (F&G < 60 → BuyLow, F&G >= 60 → SellHigh)
DCI_MIN_APY_PCT=15 (или 10 — можно снизить для большего охвата)
DCI_MAX_INVEST_USDT=20
FUNDING_ARB_ENABLED=false (включить когда будет $10+ свободных)
FUNDING_ARB_MIN_RATE=0.0001
FUNDING_ARB_MAX_USDT=30
EARN_ENABLED=true
```

## 💡 Стратегия роста капитала
```
Сейчас (~$310):     DCI + Double Win → главные генераторы
При $500+:          + Funding Rate Arb (нужно $30+ свободных)
При $1000+:         + KuCoin Lending Pro + активная торговля
При $2000+:         + CCXT multi-exchange + Binance/OKX
```

## 💡 Что уже есть и НЕ нужно переделывать
- `dci_auto_place_idle()` — ByBit DCI с VIP-fallback + capital check ✅ v10.12.4
- `dci_check_settlements()` + auto-reinvest ✅ v10.12.5
- `double_win_auto_place()` — ByBit Double Win ✅ v10.12.5
- `funding_arb_open/close/auto_check()` — готово, ждёт капитала ✅ v10.12.6
- `/api/telegram/notify` — hourly reports через quantum-bot-monitor ✅ v10.12.5b
- `/winrate` — win rate по символам из PostgreSQL ✅ v10.12.5b
- `/fundarb` — сканер funding rates + статус ✅ v10.12.6
