---
tags: [priorities, active]
date: 2026-04-05
version: v10.10.0
---
# Текущие приоритеты (обновлено 2026-04-05, v10.10.0)

## ✅ Сделано за эту сессию (v10.9.20 → v10.10.0)
1. **Дорожная карта** — обновлена до v10.9.23, Фаза 1 прогресс 65% → 80%
2. **DCI API квест решён** — `orderType="Stake"` + `accountType="FUND"` + `dualAssetsExtra{selectPrice,apyE8}`
3. **KuCoin DCI отключён** — endpoint 400100 per official docs, graceful disable
4. **DXY + S&P500** — stooq.com в macro context, MiroFish видит реальные TradFi данные
5. **ByBit Double Win** — полная интеграция, `DOUBLE_WIN_ENABLED=false` (включить когда DCI стабилен)
6. **Q-Score Auto-tune** — еженедельная самонастройка по 30д данным, bounds 72-90
7. **db.get_best_q_threshold()** — новая SQL функция для Q-tune аналитики

## 🔴 Наблюдать прямо сейчас
- **DCI первое размещение** — ждём освобождения USDT из позиций → проверить лог `[dci_mon] ✅ auto-placed`
- **4 открытые позиции** — весь торговый капитал занят, earn/DCI пропускают ($0 earnable)
- **stooq.com** — проверить что DXY/S&P в логах: `[macro] ... DXY=104.32 S&P=5421.03`

## 🟡 Следующие шаги (по приоритету)
1. **Подтвердить DCI** — дождаться первого `✅ placed BuyLow` в логах
2. **Включить Double Win** — `DOUBLE_WIN_ENABLED=true` в Railway Variables (после DCI)
3. **Auto-tune** — через ~7 дней проверить `[autotune] ✅ Q_THRESHOLD: 77 →`
4. **v10.11: Mini App / Dashboard** — Telegram WebApp, веб-панель управления

## 🟢 Работает стабильно (не трогать)
- `earn_monitor_loop` — 15 мин циклы, DCI/DW каждые 4 цикла (~60 мин)
- `mirofish_simulate()` — 15 агентов, память в PostgreSQL
- `/health` + hourly auto-report — мониторинг из телефона ✅
- `fetch_whale_movements()` + `fetch_top_traders_positions()` — работают
- `save_fg_to_history()` + `db.get_fg_history()` — F&G история в DB
- `/analyze` command — deep trade analytics

## ⚠️ Важные лимиты и границы
- `MIN_Q_SCORE` = 77 (auto-tune bounds: 72–90, delta > 2 для срабатывания)
- `DCI_MAX_INVEST_USDT` = $20 per order
- `DOUBLE_WIN_MAX_INVEST` = $20 per order
- `MAX_OPEN_POSITIONS` = 4
- `USDT Floor` = 30% от портфеля
- ARB_EXEC_ENABLED = False (hardcoded до капитала >$500)

## 📋 Backlog (когда будет капитал)
- CCXT: Binance/OKX (Wave 2A)
- Polymarket AI Trading (Wave 4)
- Mini App / Dashboard (v10.11)
- Grid Trading Passivbot
- ML агрегация сигналов

## 💡 Что уже есть и НЕ нужно переделывать
- `fetch_macro_context()` — CoinGecko + stooq DXY/S&P ✅ v10.10
- `fetch_whale_movements()` — blockchain.info ✅ v9.2
- `fetch_top_traders_positions()` — ByBit leaderboard ✅ v9.2
- `mirofish_simulate()` — 15 агентов с памятью в DB ✅ v10.0
- F&G history в PostgreSQL ✅ v9.2
- `/analyze` deep analytics ✅ v9.1
- `dci_auto_place_idle()` — ByBit DCI ✅ v10.9.23
