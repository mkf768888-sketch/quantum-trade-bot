---
tags: [bug-fix, cost, vision]
date: 2026-04-01
---
# VISION_ENABLED=false по умолчанию для экономии

## Контекст:
Vision (анализ графиков) использовал Claude Vision API → дорого.
При малом портфеле визуальный анализ не окупается.

## Решение:
```python
_vision_enabled = os.getenv("VISION_ENABLED", "false").lower() == "true"
```

По умолчанию выключено. Включить когда:
- DeepSeek будет оплачен и стабилен
- Портфель вырастет до $200+
- Будет добавлена ML модель для графиков

## Связано:
- [[убрали все Claude fallback из торговли чтобы не платить $20 в день]]
